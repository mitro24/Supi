# Supi
Supi AI supermarket helper


main.py contains the backend of the website, which creates JSON files that contain the database.

The database was created by randomly selecting a number of products from the e-katanalotis API, keeping only what was necessary for our AI helper, Supi, to work with.
